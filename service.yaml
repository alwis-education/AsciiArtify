apiVersion: v1
kind: Service
metadata:
  name: hello-world
  labels:
    app.kubernetes.io/name: helloworld
spec:
  ports:
  -   port: 8080
      targetPort: 8080
  selector:
    app.kubernetes.io/name: helloworld
